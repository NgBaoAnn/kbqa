"""Query Pipeline Orchestrator — Generate → Retrieve → Synthesize."""
# TODO: Implement run_pipeline(question, language)
#   Step 1: LLM Text-to-Cypher (generate)
#   Step 2: Neo4j execute Cypher (retrieve)
#   Step 3: LLM Data-to-Text (synthesize)
#   + Retry logic (max 2), fallback messages
