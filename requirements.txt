# AegisHealth KBQA — Python Dependencies
# ═══════════════════════════════════════
#
# Recommended: use editable install from project root:
#   pip install -e ".[dev]"
#
# This installs all deps from pyproject.toml and makes
# both ai_engine and backend importable.
#
# If you prefer requirements.txt:
#   pip install -r requirements.txt

# ── Core ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0
python-dotenv>=1.0.0

# ── Neo4j ──
neo4j>=5.20.0

# ── LightRAG ──
lightrag-hku>=1.0.0

# ── AI / ML ──
openai>=1.30.0
numpy>=1.26.0
pandas>=2.1.0

# ── HTTP ──
httpx>=0.27.0
